module common.dkeywords;

enum DKeywords {
    CREATE = "create",
    ALTER = "alter",
    WHERE = "where",
    GROUP = "group",
    BY = "by",
    JOIN = "join",
    ON = "on",
    UPDATE = "update",
}
